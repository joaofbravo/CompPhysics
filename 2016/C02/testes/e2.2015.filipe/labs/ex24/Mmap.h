#include <vector>
#include <map>
#include <string>

using namespace std;

vector< vector<int> > GetMatrix(int=1, int=1, int=0);

vector< vector<int> >& Mmapfind(map <string, vector< vector<int> > >&, string);

void Mvecprint(vector< vector<int> >&);